# ─────────────────────────────────────────────────────────────
#  CréaVidéo — Variables d'environnement (modèle)
#  Copier ce fichier en .env.local et renseigner les valeurs.
#  .env.local n'est JAMAIS committé (cf. .gitignore).
# ─────────────────────────────────────────────────────────────

# Mode mock : "true" force la simulation locale (test UI sans consommer de
# crédits). Sinon, le pipeline custom s'active si OPENROUTER_API_KEY ET
# KIE_API_KEY sont définis ; à défaut, on retombe sur le mock.
NEXT_PUBLIC_MOCK_MODE=false

# ─────────────────────────────────────────────────────────────
#  Pipeline custom (OpenRouter + ElevenLabs + Kie.ai + ffmpeg)
#  Actif si OPENROUTER_API_KEY ET KIE_API_KEY sont définis
#  (et NEXT_PUBLIC_MOCK_MODE != true). ffmpeg doit être installé
#  sur la machine hôte (macOS : brew install ffmpeg).
# ─────────────────────────────────────────────────────────────

# OpenRouter — génération du script + storyboard (modèle Claude)
OPENROUTER_API_KEY=
# Modèle utilisé via OpenRouter (optionnel)
OPENROUTER_MODEL=anthropic/claude-sonnet-4.6
# URL du site, envoyée dans l'en-tête HTTP-Referer (optionnel)
OPENROUTER_SITE_URL=

# ElevenLabs — voix off française native (appel DIRECT, pas via Kie).
# Plan PAYANT requis pour les voix de la Voice Library via API.
ELEVENLABS_API_KEY=
# Modèle TTS (optionnel)
ELEVENLABS_MODEL=eleven_multilingual_v2
# Nb de synthèses vocales simultanées (limite de requêtes concurrentes du plan ;
# Starter = 3). Au-delà, l'API renvoie HTTP 429. Optionnel, défaut 3.
ELEVENLABS_CONCURRENCE=3
# IDs de voix ElevenLabs personnalisés (optionnels, sinon défauts intégrés)
ELEVENLABS_VOIX_FEMALE=
ELEVENLABS_VOIX_FEMALE_4=
ELEVENLABS_VOIX_MALE_3=
ELEVENLABS_VOIX_MALE_4=

# Kie.ai — illustrations (Flux-2) + musique de fond (Suno)
KIE_API_KEY=
# Modèles Kie.ai (optionnels)
KIE_IMAGE_MODEL=flux-2/pro-text-to-image
# Modèle Suno pour la musique de fond (V4, V4_5, V4_5PLUS, V5, V5_5)
KIE_SUNO_MODEL=V5

# Chemins ffmpeg/ffprobe personnalisés (optionnel, sinon pris dans le PATH)
FFMPEG_PATH=
FFPROBE_PATH=

# Stockage des vidéos finales sur Supabase (optionnel).
# Sans ces variables, la vidéo est servie en local via /api/video/[jobId].
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# ─────────────────────────────────────────────────────────────
#  Tarifs pour l'estimation du coût affiché par vidéo (optionnel).
#  Valeurs en USD ; ajuste-les selon ta facturation réelle.
# ─────────────────────────────────────────────────────────────
# Taux de conversion : euros pour 1 USD
COUT_TAUX_USD_EUR=0.92
# OpenRouter (script) — USD par million de tokens (entrée / sortie)
COUT_OPENROUTER_INPUT_USD_M=3
COUT_OPENROUTER_OUTPUT_USD_M=15
# ElevenLabs (voix off) — USD pour 1000 caractères (Starter : 5 $ / 40 000 car.)
COUT_ELEVENLABS_USD_1K_CHARS=0.125
# Kie.ai — USD par image Flux-2 (5 crédits) et musique Suno (12 crédits), à 0,005 $/crédit
COUT_IMAGE_USD=0.025
COUT_MUSIQUE_USD=0.06
